---
title: "JavaScript SDK - метод getFeatureFlag"
metaTitle: "JavaScript SDK - метод getFeatureFlag"
metaDescription: "Подробности о методе getFeatureFlag из SDK OpenReplay"
---

Этот метод возвращает `IFeatureFlag` по ключу, если он существует.

## Сигнатура

```js
getFeatureFlag(): IFeatureFlag | undefined
```

## Возвращаемое значение

Возвращает Feature Flag по ключу, если он существует
