---
title: "JavaScript SDK - метод isFlagEnabled"
metaTitle: "JavaScript SDK - метод isFlagEnabled"
metaDescription: "Подробности о методе isFlagEnabled из SDK OpenReplay"
---

Этот метод возвращает `true`, если флаг с этим ключом существует и активен.

## Сигнатура

```js
isFlagEnabled(): boolean
```

## Возвращаемое значение

Возвращает `true`, если флаг существует и включён
