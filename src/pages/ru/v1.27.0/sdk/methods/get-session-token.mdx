---
title: "JavaScript SDK - метод getSessionToken"
metaTitle: "JavaScript SDK - метод getSessionToken"
metaDescription: "Подробности о методе getSessionToken из SDK OpenReplay"
---
import Aside from '~/components/Aside.astro'

<Aside type="danger">
    Этот метод **устарел**, вместо него используйте [параметр sessionHash метода `start`](/sdk/start).
</Aside>

## Сигнатура

```js 
getSessionToken(): string | null | undefined
```

## Возвращаемое значение

Возвращает хеш-токен сессии.
