---
title: "React Native SDK - метод start"
metaTitle: "React Native SDK - метод start"
metaDescription: "Подробности о методе start из React Native SDK от OpenReplay"
---

Этот метод используется для запуска трекера. При запуске трекера начинается запись сессии.

## Сигнатура

```swift
import OR from '@openreplay/react-native'

OR.tracker.startSession(options)
```

## Параметры инициализации

По умолчанию все параметры установлены в `true`:
- `crashes: Bool` Включает crashlytics.
- `analytics: Bool` Включает отслеживание аналитики помеченных представлений.
- `performances: Bool` Включает слушатель производительности
- `logs: Bool` Включает слушатель логов
- `screen: Bool` Включает запись экрана

Параметры можно использовать для отключения определённых функций; в противном случае вы можете передать пустой объект (`{}`)

## Остались вопросы?
Если у вас есть какие-либо вопросы об этом процессе, не стесняйтесь связаться с нами в нашем [Slack](https://slack.openreplay.com) или загляните на наш [Форум](https://forum.openreplay.com).