---
title: "SDK для React Native - Инициализация SDK"
metaTitle: "SDK для React Native - Инициализация SDK"
metaDescription: "Параметры конфигурации для метода-конструктора трекера React Native для iOS"
---
import Aside from '~/components/Aside.astro'
import ORCard from '~/components/Card/ORCard.astro'

<Aside type="caution">
	SDK для React Native находится в стадии бета-тестирования и в настоящее время поддерживает только приложения для iOS. Не стесняйтесь оставлять нам отзывы или сообщать о любых проблемах, с которыми вы столкнётесь, в нашем [Slack](https://slack.openreplay.com) или загляните на наш [Форум](https://forum.openreplay.com).
</Aside>

## Установка

Следующая строка установит трекер, а вместе с ним и SDK, чтобы вы могли воспользоваться всеми возможностями трекера.

```bash
npm i @openreplay/react-native
```

## Инициализация

При создании экземпляра трекера OpenReplay доступно несколько параметров конфигурации, которые вы можете задать для настройки многих аспектов записи и процесса записи. Вы должны задать параметр `projectKey` в конструкторе. Это значение можно получить на панели управления OpenReplay в разделе 'Preferences > Projects'.

<Aside type="tip">
	Обратите внимание, что если у вас несколько проектов, нужно внимательно выбирать ключ проекта.
</Aside>


### Добавление кода отслеживания

Добавьте следующий код в ваш корневой файл:

```js
import Openreplay from '@openreplay/react-native';

// ...
useEffect(() => {
	OpenReplay.tracker.startSession(
		'yourProjectKey',
		options, // explained below, set {} if empty
		'https://local.openreplay.instance/ingest' // not required if you're using managed version
	);
}, [])
```

### Очистка данных

Подробнее см. [как очищать данные в повторах React Native](/rn-sdk/sanitization).

## Параметры инициализации

По умолчанию все параметры установлены в `true`:

- `crashes: Bool` Включает crashlytics.
- `analytics: Bool` Включает отслеживание аналитики помеченных представлений.
- `performances: Bool` Включает слушатель производительности.
- `logs: Bool` Включает слушатель логов.
- `screen: Bool` Включает запись экрана.

## Методы

<div class="grid grid-cols-12 md:grid-cols-12 gap-3 md:gap-4">
	<ORCard title="Познакомьтесь с методами и модулями SDK для React Native" url="/rn-sdk/modules" hideIcon={true} width="100"/>
</div>

## Остались вопросы?
Если у вас есть какие-либо вопросы об этом процессе, не стесняйтесь обращаться к нам в нашем [Slack](https://slack.openreplay.com) или загляните на наш [Форум](https://forum.openreplay.com).