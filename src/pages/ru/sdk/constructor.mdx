Заголовок: «JavaScript SDK - Инициализация SDK»
Мета-заголовок: «JavaScript SDK - Инициализация SDK»
Мета-описание: «Опции конфигурации для метода конструктора трекера»

## Установка

Следующая строка установит трекер и с ним SDK, чтобы вы могли воспользоваться всеми функциями трекера.

## Инициализация
При создании трекера OpenReplay вы можете предоставить несколько опций конфигурации, чтобы настроить многие аспекты записи и опыта записи.

Вы должны установить опцию `projectKey` в конструкторе. Это значение можно получить из панели управления OpenReplay в разделе «Настройки> Проекты».

<Aside type="tip">
Обратите внимание, что если у вас несколько проектов, вам нужно быть осторожным с выбором ключа проекта.
</Aside>

### Одностраничные приложения (SPA)
Если ваш сайт является **одностраничным приложением (SPA)**, используйте код ниже:

### Серверно-отрисованные (SSR)
В противном случае, если ваше веб-приложение **серверно-отрисованное (SSR)** (т. е. NextJS, NuxtJS), используйте нижеприведенный фрагмент. Убедитесь, что `tracker.start()` вызывается после запуска приложения (в `useEffect` или `componentDidMount`).

<Aside type="tip">
Если вы используете определенный веб-фреймворк, ознакомьтесь с разделом [Настройка трекера](/using-or), чтобы узнать, как начать.
</Aside>

## Пример

## Опции инициализации

Есть набор опций, которые можно передать в конструктор. Только `projectKey` является обязательным.

### Общее назначение
- `projectKey: string` ID проекта, который вы отслеживаете.
- `sessionHash?: string` Хэш начальной сессии. Это полезно, когда сессии пересекают разные поддомены вашего веб-приложения, но вы хотите сшить их в одну запись. В случае, если невозможно продолжить сессию (она не существует или завершена), трекер автоматически начнет новую. Он также возвращается на `stop()`. Более подробно об этом здесь.
- `ingestPoint?: string` Ваш домен OpenReplay (т. е. https://openreplay.mydomain.com/ingest), к которому трекер будет отправлять события. Это необязательно для пользователей OpenReplay Cloud. По умолчанию: `https://api.openreplay.com/ingest` (что указывает на OpenReplay Cloud).
- `revID?: string` Идентификатор ревизии вашего веб-приложения. Полезно при поиске проблем, возникающи
```bash
npm i @openreplay/tracker
```
