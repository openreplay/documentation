---
title: "Развёртывание на Ubuntu"
metaTitle: "Развёртывание на Ubuntu"
metaDescription: "Пошаговое руководство по развёртыванию OpenReplay на любой машине с Ubuntu."
layout: ~/layouts/MainLayout.astro
i18nReady: true
---

Стек OpenReplay можно установить на одну машину под управлением Ubuntu. Вот как это сделать.

## Предварительные требования

- Операционная система должна быть *Ubuntu Server 22.04 Focal Fossa*.
- Минимальные характеристики машины, на которой работает OpenReplay, — `2 vCPUs, 8 GB of RAM, 50 GB of storage`, иначе серверные сервисы OpenReplay просто не запустятся. Этого должно быть достаточно для низкого/умеренного объёма. Если вы ожидаете высокий трафик, вам следует масштабироваться, исходя из этого.
- Публичный IP-адрес, указывающий на вашу машину.

## Развёртывание OpenReplay

1. Убедитесь, что ваша машина запущена, затем подключитесь к ней по SSH как root

2. Установите OpenReplay, указав домен, на котором он будет работать (например, DOMAIN_NAME=openreplay.mycompany.com):

```bash
sudo wget https://raw.githubusercontent.com/openreplay/openreplay/main/scripts/helmcharts/openreplay-cli -O /bin/openreplay 
sudo chmod +x /bin/openreplay
openreplay -i DOMAIN_NAME
```

## Настройка TLS/SSL

OpenReplay работает с конфиденциальными данными пользователей и поэтому требует HTTPS для работы. Это обязательно, иначе трекер просто не начнёт запись. То же самое касается панели управления: без HTTPS вы не сможете воспроизводить пользовательские сессии.

Поэтому вы должны предоставить (или сгенерировать) свой собственный SSL-сертификат.

1. Сначала перейдите к своему поставщику услуг DNS, отредактируйте свою DNS-зону и создайте `A Record`. Используйте домен, который вы указали ранее на этапе установки, и направьте его на машину, используя её публичный IP.

2. Если вы предоставляете свой собственный сертификат, создайте SSL-секрет с помощью следующей команды: `kubectl create secret tls openreplay-ssl -n app --key="private_key_file.pem" --cert="certificate.crt"`.

> **Примечание:** Если у вас нет сертификата, сгенерируйте автоматически обновляемый сертификат для вашего поддомена (того, который вы указали при установке) с помощью Let's Encrypt. Просто подключитесь к машине OpenReplay, выполните `cd /var/lib/openreplay/openreplay/scripts/helmcharts && bash certmanager.sh` и следуйте инструкциям.

3. Если вы хотите включить перенаправление с http на https (рекомендуется), раскомментируйте приведённый ниже блок в разделе `ingress-nginx` в файле `/var/lib/openreplay/vars.yaml`:
   
```yaml
ingress-nginx: &ingress-nginx
  controller:
    config:
      ssl-redirect: true
      force-ssl-redirect: true
```

Стоит отметить, что наш `ingress-nginx` по умолчанию работает на портах `80|443`, но при необходимости это можно легко изменить в `vars.yaml`:

```yaml
ingress-nginx: &ingress-nginx
  controller:
    service:
      ports:
        http: 80
        https: 443
```

4. Наконец, переустановите OpenReplay NGINX:

```bash
openreplay -R
```

Теперь всё готово, OpenReplay должен быть доступен на вашем поддомене. Вы можете создать учётную запись, посетив страницу `/signup` (т. е. openreplay.mycompany.com/signup).

## Есть вопросы?

Если вы столкнулись с какими-либо проблемами, присоединяйтесь к нашему [сообществу в Slack](https://slack.openreplay.com/) и получите помощь от наших разработчиков.
