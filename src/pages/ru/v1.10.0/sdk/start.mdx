---
Заголовок: «JavaScript SDK - метод «start»
MetaTitle: «JavaScript SDK - метод «start»
MetaDescription: «Детали о методе «start» из SDK OpenReplay
---

Этот метод используется для запуска трекера. 
Запуская трекер, начинается запись сессии.

## Сигнатура

```js
start(startOpts?: Partial<StartOptions>): Promise<StartPromiseReturn>
```

