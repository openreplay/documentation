['---\ntitle: "Настройка OpenReplay"\nmetaTitle: "Настройка OpenReplay"\nmetaDescription: "Как быстро настроить OpenReplay и начать запись сеансов и метрик."\n---\nimport YoutubeVideo from \'~/components/YoutubeVideo.astro\'\nimport IconText from \'~/components/Card/IconText.astro\'\n\n<YoutubeVideo title="Смотрите, как начать работу с OpenReplay"\n  description="Если вам не нравится читать, вы можете следовать этому видеоуроку, показывающему, как начать работу с OpenReplay за 3 минуты или меньше"\n  videoURL="https://www.youtube.com/watch?v=EWbtMK3qK30"\n  />\n\n\nНачать работу с OpenReplay просто. \n\nЕсли вы хотите посмотреть быстрый учебник по началу работы, используйте [это видео YouTube](https://www.youtube.com/watch?v=EWbtMK3qK30), иначе продолжайте читать для получения дополнительной информации.\n\nСуществует 2 способа запуска OpenReplay: с помощью скрипта или пакета npm.\n\n## Использование скрипта\n\nСкопируйте / вставьте фрагмент кода непосредственно в `</head>` вашего веб-приложения. Скрипт можно найти в панели управления OpenReplay в разделе «Настройки> Проекты» и уникален для каждого проекта.\n\nЕсли ваш сайт поддерживает Google Tag Manager, то вы можете следовать [руководству GTM по шагам](/integrations/google-tag-manager).\n\n## Использование NPM\n\nСначала установите пакет npm.\n\n
```bash
npm i @openreplay/tracker
```
', "\n\nЗатем инициализируйте пакет из точки входа в ваш код и запустите трекер. Вы должны установить опцию `projectKey` в конструкторе. Его значение можно найти в панели управления OpenReplay в разделе «Настройки> Проекты».\n\n### Если ваш сайт является одностраничным приложением (SPA)\n\n
```js
import OpenReplay from '@openreplay/tracker';
//...
const tracker = new OpenReplay({
  projectKey: PROJECT_KEY
});
tracker.start();
```
", '\n\n### Если ваше веб-приложение отрисовывается на стороне сервера (SSR)\n\nСледуйте нижеприведенному фрагменту, если ваше веб-приложение отрисовывается на стороне сервера (SSR) (т. е. NextJS, NuxtJS). Убедитесь, что `tracker.start()` вызывается один раз после запуска приложения (в `useEffect` или `componentDidMount`).\n\n
```js
import OpenReplay from '@openreplay/tracker/cjs';
//...
const tracker = new OpenReplay({
      projectKey: PROJECT_KEY
});
//...
function MyApp() {
  useEffect(() => { // use componentDidMount in case of React Class Component
    tracker.start();
  }, []);
}
```
']
