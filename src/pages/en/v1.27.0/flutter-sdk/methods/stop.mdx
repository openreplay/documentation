---
title: "Flutter SDK - stop method"
metaTitle: "Flutter SDK - stop method"
metaDescription: "Details about the 'stop' method from OpenReplay's Flutter SDK"
---
import Aside from '~/components/Aside.astro'

This method is used to stop the tracker. By stopping the tracker, the recording of the session stops.

## Signature

```dart
Future<void> OpenReplay.instance.stop()
```

`stop()` is a teardown: it stops every listener, flushes and **discards** the buffers, and clears the session.

<Aside type="note">
	You don't need to call this when the app is backgrounded. The SDK observes the app lifecycle itself, pausing capture and flushing on background and resuming on foreground, while keeping the buffers.
</Aside>

## Have questions?
If you have any questions about this process, feel free to reach out to us on our [Slack](https://slack.openreplay.com) or check out our [Forum](https://forum.openreplay.com).
