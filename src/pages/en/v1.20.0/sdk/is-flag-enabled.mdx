---
title: "JavaScript SDK - isFlagEnabled method"
metaTitle: "JavaScript SDK - isFlagEnabled method"
metaDescription: "Details about the isFlagEnabled method from OpenReplay's SDK"
---

This method returns `true` if flag with this key does exist and is active.

## Signature

```js
isFlagEnabled(): boolean
```

## Return value

Returns `true` if flag exists and is enabled
