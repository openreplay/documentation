---
title: "JavaScript SDK - 'forceFlushBatch' method"
metaTitle: "JavaScript SDK - 'forceFlushBatch' method"
metaDescription: "Details about the 'forceFlushBatch' method from OpenReplay's SDK"
---

Forces tracker to flush current batch of messages without waiting for completion. Useful when you need to close the tab/window after session.

Will send a message to webworker, which will start network request with current batch of messages.

## Signature

```ts
forceFlushBatch(): void
```
