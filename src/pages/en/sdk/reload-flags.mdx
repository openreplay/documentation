---
title: "JavaScript SDK - reloadFlags method"
metaTitle: "JavaScript SDK - reloadFlags method"
metaDescription: "Details about the reloadFlags method from OpenReplay's SDK"
---

This method reloads all flags (can be useful if user session had any change of metadata, userId or any other information that could be required for certain flag conditions).

## Signature

```js
reloadFlags(): string | null | undefined+
```

## Return value

Returns the ID of the session being recorded.