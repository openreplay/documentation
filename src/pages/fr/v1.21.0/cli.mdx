---
title: "CLI d'OpenReplay"
metaTitle: "CLI d'OpenReplay"
metaDescription: "Détails sur l'utilitaire CLI d'OpenReplay"
minVersion: "1.10.0"
---

À partir de la v1.10.0, nous avons publié notre utilitaire CLI. Grâce à lui, les tâches les plus courantes que vous auriez à exécuter pour gérer votre version auto-hébergée d'OpenReplay peuvent désormais être réalisées avec une simple commande.


## Installation de la CLI

La CLI est fournie avec chaque version auto-hébergée d'OpenReplay.

## Utilisation de la CLI

La CLI possède la syntaxe suivante :

```bash 
openreplay [command] <service>
```

La liste des commandes disponibles est la suivante :

- `-h or --help` : Cette commande affichera la liste des commandes et des services disponibles.
- `-s or --status` : Si vous voulez connaître l'état des services déployés, cette commande vous donnera tout ce que vous avez besoin de savoir.
- `-u or --upgrade` : Utilisée pour mettre à niveau votre installation vers la dernière version disponible.
- `-U or --deprecated-upgrade /path/to/old_vars.yaml`
- `-r or --restart` : Vous permet de redémarrer tous les services. Ce faisant, vous pouvez corriger les services qui ne répondent pas.
- `-R or --Reload` : Recharge la configuration d'un service, donc utilisez-la uniquement lorsque vous modifiez quelque chose.
- `-p or --install-packages` : Met à jour toutes les dépendances tierces (comme `kubectl`, `helm`, etc).
- `-i or --install domain.name.com` : Installe OpenReplay sur la machine.
- `-e or --edit` : Modifie la configuration installée d'OpenReplay et la recharge.
- `-l or --logs SERVICE` : Affiche les journaux d'un service en particulier.

La liste de tous les services disponibles pour la CLI est la suivante :

- alerts
- assets
- assist
- chalice
- db
- ender
- frontend
- heuristics
- http
- integrations
- ngix-controller
- peers
- sink
- sourcemapreader
- storage
- image-storage
- video-storage

## Exemple

Pour obtenir l'état du déploiement, exécutez la commande CLI ci-dessous :

```bash 
openreplay -s 
```

Le résultat ressemblerait à quelque chose comme ceci :

![OpenReplay status](./cli/images/or-cli-status.png)

## Vous avez des questions ?

Si vous rencontrez des problèmes, connectez-vous à notre [Slack](https://slack.openreplay.com) ou consultez notre [Forum](https://forum.openreplay.com) et obtenez de l'aide de notre communauté.
