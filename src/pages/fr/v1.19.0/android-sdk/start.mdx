---
title: "Android SDK - méthode start"
metaTitle: "Android SDK - méthode start"
metaDescription: "Détails sur la méthode 'start' du SDK Android d'OpenReplay"
---

Cette méthode est utilisée pour démarrer le tracker. En démarrant le tracker, l'enregistrement de la session commence.

## Signature

```kotlin
OpenReplay.start(applicationContext: Context, projectKey: String, options: OROptions)
	```

### Options d'initialisation

Options disponibles :

- `crashes: Bool` Active crashlytics.
- `analytics: Bool` Active l'analytique pour les vues.
- `performances: Bool` Active le listener de performance
- `logs: Bool` Active le listener de logs
- `screen: Bool` Active l'enregistreur d'écran
- `wifiOnly: Bool` Force le tracker à démarrer uniquement si l'utilisateur dispose d'une connexion wifi

Par défaut : tout est à `true`

	
