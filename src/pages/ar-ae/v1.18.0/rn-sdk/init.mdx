---
title: "React Native SDK - تهيئة الـ SDK"
metaTitle: "React Native SDK - تهيئة الـ SDK"
metaDescription: "خيارات التهيئة لطريقة المُنشئ (constructor) الخاصة بمتعقّب React Native لنظام iOS"
---
import Aside from '~/components/Aside.astro'
import ORCard from '~/components/Card/ORCard.astro'

<Aside type="caution">
	إنّ React Native SDK في مرحلة الإصدار التجريبي (Beta). لا تتردّد في تزويدنا بملاحظاتك أو الإبلاغ عن أي مشكلة قد تواجهها عبر [Slack](https://slack.openreplay.com) الخاص بنا أو الاطلاع على [المنتدى](https://forum.openreplay.com).
</Aside>

## التثبيت

سيقوم السطر التالي بتثبيت المتعقّب ومعه الـ SDK لتتمكّن من الاستفادة من جميع ميزات المتعقّب.

```bash
npm i @openreplay/react-native
```

## التهيئة

عند إنشاء نسخة من متعقّب OpenReplay، تتوفّر عدّة خيارات تهيئة يمكنك توفيرها لتخصيص العديد من جوانب التسجيل وتجربة التسجيل. يجب عليك تعيين خيار `projectKey` في المُنشئ. يمكنك الحصول على هذه القيمة من لوحة تحكّم OpenReplay الخاصة بك ضمن 'Preferences > Projects'.

<Aside type="tip">
	لاحظ أنّه إذا كان لديك عدّة مشاريع، فعليك توخّي الحذر عند اختيار مفتاح المشروع.
</Aside>


### إضافة كود التتبّع

أضف الكود التالي إلى ملفك الجذري:

```js
import Openreplay from '@openreplay/react-native';

// ...
useEffect(() => {
	OpenReplay.tracker.startSession(
		'yourProjectKey',
		options, // explained below, set {} if empty
		'https://local.openreplay.instance/ingest' // not required if you're using managed version
	);
}, [])
```

### تنقية البيانات

راجع [كيفية تنقية البيانات في عمليات إعادة التشغيل الخاصة بـ React Native](/rn-sdk/sanitization) لمزيد من التفاصيل.

## خيارات التهيئة

افتراضيًا، يتمّ تعيين جميع الخيارات على `true`:

- `crashes: Bool` يُفعّل crashlytics.
- `analytics: Bool` يُفعّل التتبّع التحليلي للعروض المُعلَّمة.
- `performances: Bool` يُفعّل مستمع الأداء.
- `logs: Bool` يُفعّل مستمع السجلّات.
- `screen: Bool` يُفعّل مسجّل الشاشة.

## الطرق

<div class="grid grid-cols-12 md:grid-cols-12 gap-3 md:gap-4">
	<ORCard title="تعرّف على طرق ووحدات React Native SDK" url="/rn-sdk/modules" hideIcon={true} width="100"/>
</div>
