---
title: "React Native SDK - método start"
metaTitle: "React Native SDK - método start"
metaDescription: "Detalles sobre el método start del SDK de React Native de OpenReplay"
---

Este método se utiliza para iniciar el rastreador. Al iniciar el rastreador, comienza la grabación de la sesión.

## Firma

```swift
import OR from '@openreplay/react-native'

OR.tracker.startSession(options)
```

## Opciones de inicialización

De forma predeterminada, todas las opciones están marcadas como `true`:
- `crashes: Bool` Habilita crashlytics.
- `analytics: Bool` Habilita el seguimiento de analítica de las vistas marcadas.
- `performances: Bool` Habilita el listener de rendimiento
- `logs: Bool` Habilita el listener de logs
- `screen: Bool` Habilita el grabador de pantalla

Las opciones pueden usarse para deshabilitar ciertas funciones; de lo contrario, puedes pasar el objeto vacío (`{}`)
