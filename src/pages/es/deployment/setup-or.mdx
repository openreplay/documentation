---
title: "Configurar OpenReplay"
metaTitle: "Configurar OpenReplay"
metaDescription: "Cómo configurar OpenReplay rápidamente y empezar a grabar sesiones y métricas."
---
import YoutubeVideo from '~/components/YoutubeVideo.astro'
import LinkRow from '~/components/LinkRow.astro'

<YoutubeVideo title="Mira cómo empezar a usar OpenReplay"
  description="Si no te gusta leer, puedes seguir este tutorial en vídeo que te muestra cómo empezar con OpenReplay en 3 minutos o menos"
  videoURL="https://www.youtube.com/watch?v=EWbtMK3qK30"
  />


Empezar a usar OpenReplay es sencillo. 

Si quieres ver un tutorial rápido sobre cómo empezar, usa [este vídeo de YouTube](https://www.youtube.com/watch?v=EWbtMK3qK30); de lo contrario, sigue leyendo para obtener más detalles.

Hay 2 formas de poner OpenReplay en funcionamiento: usando el script o el paquete npm.

## Usando el Script

Copia y pega el fragmento de código directamente en el `</head>` de tu aplicación web. El script se encuentra en el panel de OpenReplay en 'Preferences > Projects' y es único para cada proyecto.

Si tu sitio web es compatible con Google Tag Manager, puedes seguir la [guía paso a paso de GTM](/integrations/google-tag-manager).

## Usando NPM

Primero instala el paquete npm.

```bash
npm i @openreplay/tracker
```

Luego, inicializa el paquete desde el punto de entrada de tu base de código e inicia el tracker. Debes establecer la opción `projectKey` en el constructor. Su valor se encuentra en tu panel de OpenReplay en 'Preferences > Projects'.

### Si tu sitio web es una Single Page Application (SPA)

Puedes usar el tracker como una instancia singleton, que es la forma recomendada de usarlo. 
De esta manera, puedes acceder al tracker desde cualquier parte de tu aplicación:

```js
import { tracker } from '@openreplay/tracker'

tracker.configure({
	projectKey: PROJECT_KEY,
  ingestPoint: "https://openreplay.mydomain.com/ingest", // when dealing with the self-hosted version of OpenReplay
})

tracker.start() // returns a promise with session info (sessionID, sessionHash, userUUID)
```

O puedes usar la clase `Tracker` directamente para manejar su instancia manualmente:

```js
import OpenReplay from '@openreplay/tracker';
//...
const tracker = new OpenReplay({
  projectKey: PROJECT_KEY,
  ingestPoint: "https://openreplay.mydomain.com/ingest" // only required if using the self-hosted version of OpenReplay
});

tracker.start();
```

### Si tu aplicación web se renderiza en el servidor (SSR)

Sigue el siguiente fragmento si tu aplicación web se renderiza en el servidor (SSR) (es decir, NextJS, NuxtJS). 
Asegúrate de que el tracker se ejecute en un entorno de navegador y que `tracker.start()` se llame una vez que la aplicación se sirva al cliente (es decir, en `useEffect` o `componentDidMount`).

```js
import OpenReplay from '@openreplay/tracker/cjs';
//...
const tracker = new OpenReplay({
      projectKey: PROJECT_KEY
});
//...
function MyApp() {
  useEffect(() => { // use componentDidMount in case of React Class Component
    tracker.start();
  }, []);
}
```

Si deseas profundizar en la configuración del tracker, consulta el [SDK de JavaScript](/installation/javascript-sdk#options) para ver la lista de opciones disponibles.

## Integración con otros frameworks
Si no estás usando OpenReplay dentro de una aplicación básica, es probable que estés usando un framework de desarrollo web, como Next.js, Nuxt u otro.
Consulta nuestros tutoriales personalizados para cada uno de los frameworks compatibles:

<div class="or-feature">
  <div class="or-chips" style="margin:0;">
    <a class="or-chip" href="/sdk/using-or/react"><img src="/redesign/frameworks/react.svg" alt="" />React</a>
    <a class="or-chip" href="/sdk/using-or/vue"><img src="/redesign/frameworks/vue.svg" alt="" />Vue</a>
    <a class="or-chip" href="/sdk/using-or/angular"><img src="/redesign/frameworks/angular.svg" alt="" />Angular</a>
    <a class="or-chip" href="/sdk/using-or/next"><img src="/redesign/frameworks/nextjs.svg" alt="" />NextJS</a>
    <a class="or-chip" href="/sdk/using-or/nuxt"><img src="/redesign/frameworks/nuxt.svg" alt="" />Nuxt</a>
  </div>
</div>

## Próximos pasos

Ahora que has instalado el tracker y desplegado tu aplicación web, las grabaciones de sesiones deberían aparecer en pocos minutos. ¡Bien hecho!

Mientras tanto, puedes avanzar en el proceso de configuración para sacar el máximo partido de OpenReplay. Cuanto más captures, mejor para reproducir y corregir problemas rápidamente:

<div class="or-rowlist">
  <LinkRow href="/installation/identify-user" title="Identificar a un usuario" icon="user" desc="Asocia tu ID interno de usuario con la sesión que se está grabando." />
  <LinkRow href="/installation/metadata" title="Metadatos" icon="metadata" desc="También conocidos como traits o variables de usuario, los metadatos proporcionan información adicional sobre los usuarios durante la grabación de sesiones." />
  <LinkRow href="/installation/custom-events" title="Eventos personalizados" icon="api" desc="Añade más inteligencia registrando eventos específicos del dominio junto con las repeticiones de sesiones." />
  <LinkRow href="/installation/error-reporting" title="Informe de errores" icon="error" desc="Reporta errores y excepciones y míralos en la pestaña ERRORS." />
  <LinkRow href="/installation/upload-sourcemaps" title="Subir Source-Maps" icon="highlight" desc="Visualiza el contexto del código fuente obtenido de los stack traces en su forma original." />
  <LinkRow href="/plugins" title="Plugins" icon="settings" desc="Comprende qué sucede internamente capturando el store de tu aplicación y monitoreando tus consultas HTTP y GraphQL." />
  <LinkRow href="/integrations" title="Integraciones" icon="data" desc="Conecta OpenReplay con tus herramientas de registro y sincroniza los errores del backend con las repeticiones de sesiones." />
</div>
