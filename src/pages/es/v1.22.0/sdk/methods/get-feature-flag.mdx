---
title: "SDK de JavaScript - método getFeatureFlag"
metaTitle: "SDK de JavaScript - método getFeatureFlag"
metaDescription: "Detalles sobre el método getFeatureFlag del SDK de OpenReplay"
---

Este método devuelve un `IFeatureFlag` por clave si existe.

## Firma

```js
getFeatureFlag(): IFeatureFlag | undefined
```

## Valor de retorno

Devuelve la Feature Flag por clave si existe
