---
title: "Enviar Metadatos"
metaTitle: "Metadatos"
metaDescription: "Cómo pasar metadatos de usuario a OpenReplay. También conocidos como traits o variables de usuario en otras plataformas."
---
import Aside from '~/components/Aside.astro'

Los metadatos te permiten añadir información adicional sobre los usuarios a las repeticiones de sesión en forma de pares clave-valor. Esta funcionalidad mejora tu capacidad de filtrar y buscar grabaciones de sesión, facilitando la identificación de las sesiones relevantes. Consulta este [tutorial](/tutorials/metadata) para obtener más detalles sobre cómo enviar metadatos.

<Aside>

**Las claves de metadatos son específicas de cada proyecto**: las claves de metadatos deben definirse en el dashboard antes de que puedas enviar metadatos a OpenReplay. Cada proyecto puede tener su propio conjunto de claves de metadatos.

</Aside>

## Añadir claves de metadatos a un proyecto

Para configurar las claves de metadatos:

1. Ve a **Preferences > Metadata** en OpenReplay.
2. Elige el proyecto al que quieres añadir metadatos.
3. Haz clic en **Add Metadata**.
4. Introduce el nombre de clave deseado en el campo **Field Name**.
5. Haz clic en **Add** para guardar la clave de metadatos.

<div style={{ display: 'flex', justifyContent: 'center', alignItems: 'center' }}>
  <img src="/installation/send-metadata/send_metadata.gif" alt="Enviar Metadatos" style={{ width: '600px', height: 'auto' }} />
</div>


> **Límite:** Puedes añadir hasta 10 claves de metadatos por proyecto.

## Inyectar metadatos en las repeticiones de sesión

Una vez definidas tus claves de metadatos, puedes enviar metadatos al grabar sesiones. A continuación se muestran los métodos de implementación disponibles:

### 1. Usando NPM

Inyecta metadatos al iniciar el tracker:

```js
const tracker = new OpenReplay({
  projectKey: PROJECT_KEY
});

tracker.start({
  userID: "john@doe.com",
  metadata: {
    balance: "10M",
    plan: "free"
  }
});
```

Si los metadatos solo están disponibles después de que el tracker se inicie, usa el método `setMetadata`:

```javascript
tracker.setMetadata('plan', 'free'); // after tracker.start()
```

### 2. Usando el snippet de JavaScript

Para incluir metadatos con el snippet, modifica la variable `startOpts`:

```js
<!-- OpenReplay Tracking Code -->
<script>
  var initOpts = {
    projectKey: "project_key",
    defaultInputMode: 2,
    obscureTextNumbers: false,
    obscureTextEmails: true,
  };
  const yourMetadata = { yourKey: 'yourValue' };
  var startOpts = { userID: "", metadata: yourMetadata };
  (function(A,s,a,y,e,r){
    r=window.OpenReplay=[e,r,y,[s-1, e]];
    s=document.createElement('script');s.src=A;s.async=!a;
    ...
  })("//static.openreplay.com/latest/openreplay.js",1,0,initOpts,startOpts);

</script>
```

Si los metadatos están disponibles más adelante:

```html
<script>
  OpenReplay.setMetadata("plan", "free");
</script>
```

### 3. Usando el SDK de iOS

Inyecta metadatos en tu aplicación de iOS:

```swift
import ORTracker

ORTracker.shared.setMetadata(key: String, value: String)
```

## Filtrar y buscar sesiones con metadatos

Las claves de metadatos te permiten filtrar y buscar sesiones en OpenReplay:

1. Abre la **Omni-Search** en la vista de Sessions.
2. Selecciona tu clave de metadatos (por ejemplo, `plan`).
3. Introduce el valor del metadato (por ejemplo, `free`) para filtrar las grabaciones de sesión.

<div style={{ display: 'flex', justifyContent: 'center', alignItems: 'center' }}>
  <img src="/installation/send-metadata/filter_metatdata.gif" alt="Filtrar Metadatos" style={{ width: '600px', height: 'auto' }} />
</div>

Esta funcionalidad permite identificar sesiones de forma rápida y precisa según detalles específicos del usuario.

## ¿Tienes preguntas?
Si tienes alguna pregunta sobre este proceso, no dudes en contactarnos en nuestro [Slack](https://slack.openreplay.com) o consulta nuestro [Foro](https://forum.openreplay.com).
