---
title: "SDK de Analítica del Tracker"
metaTitle: "Uso del SDK de analítica del tracker de OpenReplay"
metaDescription: "Aprende a configurar y utilizar el SDK de analítica del tracker de OpenReplay"
---
import LinkRow from '~/components/LinkRow.astro'
import IconText from '~/components/Card/IconText.astro'

## SDK de Analítica

Aprende a configurar el SDK de analítica del tracker de OpenReplay.

## Módulos

<div class="or-rowlist">
  <LinkRow href="/sdk/analytics/people" title="People" icon="user" desc="Gestiona los perfiles y atributos de los usuarios." />
  <LinkRow href="/sdk/analytics/events" title="Events" icon="api" desc="Rastrea y analiza las interacciones y eventos de los usuarios." />
</div>

## Firmas de nivel superior

```ts
// Identify a user with an id (e.g. email, username, etc.)
tracker.analytics.identify(userId: string): void
// same as
tracker.analytics.people.identify()

// Add event to batch with option to send it immediately
tracker.analytics.track(eventName: string, tempProperties?: Record<string, any>, options?: { send_immediately: boolean })
// same as
tracker.analytics.events.track()
```

## ¿Tienes preguntas?

Si encuentras algún problema, conéctate a nuestro [Slack](https://slack.openreplay.com) o visita nuestro [Foro](https://forum.openreplay.com) y obtén ayuda de nuestra comunidad.
