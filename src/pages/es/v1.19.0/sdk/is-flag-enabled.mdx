---
title: "SDK de JavaScript - método isFlagEnabled"
metaTitle: "SDK de JavaScript - método isFlagEnabled"
metaDescription: "Detalles sobre el método isFlagEnabled del SDK de OpenReplay"
---

Este método devuelve `true` si existe una bandera con esta clave y está activa.

## Firma

```js
isFlagEnabled(): boolean
```

## Valor de retorno

Devuelve `true` si la bandera existe y está habilitada
