---
title: "SDK de Flutter - método setMetadata"
metaTitle: "SDK de Flutter - método setMetadata"
metaDescription: "Detalles sobre el método setMetadata del SDK de Flutter de OpenReplay"
---
import Aside from '~/components/Aside.astro'

Con este método, puedes establecer el valor de uno de los campos de metadatos configurados en la plataforma de OpenReplay.

<Aside type="note">
	Puedes llamar a este método tantas veces como desees, solo ten en cuenta que si usas el mismo atributo `key`, el último valor que pasaste será el que se guarde en la plataforma. Todos los valores anteriores se perderán.
</Aside>

Consulta [metadatos](/session-replay/metadata) para más detalles.

## Firma
```dart
OpenReplay.instance.setMetadata(String key, String value)
```

## Parámetros

- `key: String`: La clave de metadatos a guardar.
- `value: String`: El valor a guardar como metadatos.

## Valor de retorno

Este método no devuelve ningún valor. La información se guarda directamente como parte de la reproducción y se puede ver dentro de la plataforma de OpenReplay.

## ¿Tienes preguntas?
Si tienes alguna pregunta sobre este proceso, no dudes en contactarnos en nuestro [Slack](https://slack.openreplay.com) o consulta nuestro [Foro](https://forum.openreplay.com).
