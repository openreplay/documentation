---
title: "JavaScript SDK - isFlagEnabled 方法"
metaTitle: "JavaScript SDK - isFlagEnabled 方法"
metaDescription: "关于 OpenReplay SDK 中 isFlagEnabled 方法的详细信息"
---

如果存在使用此键的标志且该标志处于激活状态，此方法返回 `true`。

## 签名

```js
isFlagEnabled(): boolean
```

## 返回值

如果标志存在且已启用，则返回 `true`
