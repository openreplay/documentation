---
title: "React Native SDK - 初始化 SDK"
metaTitle: "React Native SDK - 初始化 SDK"
metaDescription: "iOS React Native 跟踪器构造方法的配置选项"
---
import Aside from '~/components/Aside.astro'
import ORCard from '~/components/Card/ORCard.astro'

<Aside type="caution">
	React Native SDK 目前处于 Beta 阶段，仅支持 iOS 应用。欢迎在我们的 [Slack](https://slack.openreplay.com) 上向我们提供反馈或报告您遇到的任何问题，或查看我们的[论坛](https://forum.openreplay.com)。
</Aside>

## 安装

以下命令将为您安装跟踪器以及随附的 SDK，让您能够使用所有跟踪器功能。

```bash
npm i @openreplay/react-native
```

## 初始化

在实例化 OpenReplay 的跟踪器时，您可以提供多个配置选项来自定义录制及录制体验的诸多方面。您必须在构造函数中设置 `projectKey` 选项。您可以在 OpenReplay 仪表板的 'Preferences > Projects' 下获取该值。

<Aside type="tip">
	请注意，如果您有多个项目，需要谨慎选择使用哪个项目密钥。
</Aside>


### 添加跟踪代码

将以下代码添加到您的根文件中：

```js
import Openreplay from '@openreplay/react-native';

// ...
useEffect(() => {
	OpenReplay.tracker.startSession(
		'yourProjectKey',
		options, // explained below, set {} if empty
		'https://local.openreplay.instance/ingest' // not required if you're using managed version
	);
}, [])
```

### 数据脱敏

有关更多详情，请参阅[如何在 React Native 回放中对数据进行脱敏](/rn-sdk/sanitization)。

## 初始化选项

默认情况下，所有选项均标记为 `true`：

- `crashes: Bool` 启用崩溃分析（crashlytics）。
- `analytics: Bool` 启用对已标记视图的分析跟踪。
- `performances: Bool` 启用性能监听器。
- `logs: Bool` 启用日志监听器。
- `screen: Bool` 启用屏幕录制器。

## 方法

<div class="grid grid-cols-12 md:grid-cols-12 gap-3 md:gap-4">
	<ORCard title="了解 React Native SDK 的方法和模块" url="/rn-sdk/modules" hideIcon={true} width="100"/>
</div>

## 有疑问？
如果您对此流程有任何疑问，欢迎通过我们的 [Slack](https://slack.openreplay.com) 与我们联系，或查看我们的[论坛](https://forum.openreplay.com)。