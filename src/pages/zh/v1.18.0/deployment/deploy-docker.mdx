---
layout: ~/layouts/MainLayout.astro
i18nReady: true
title: "使用 Docker Compose 部署"
metaTitle: "使用 Docker Compose 部署"
metaDescription: "使用 Docker Compose 部署 OpenReplay 的分步指南。"
---
import Aside from '~/components/Aside.astro'

<Aside type="tip">
	这是一项实验性功能，其实现方式将来可能会有所变化。
</Aside>

OpenReplay 技术栈可以使用 Docker Compose 安装在单台机器上。 

## 前提条件

1. Ubuntu 20.04 LTS（推荐使用 LTS 版本）
2. 为机器分配公网 IP。这意味着你的服务器可以从互联网访问。
3. 你应该有一个绑定到该 IP 地址的域名。
4. 选择你的实例类型。最低规格为 `2 vCPUs, 8 GB of RAM, 50 GB of storage`，否则 OpenReplay 后端服务根本无法启动。因此，我们建议**至少**使用 `t3.large`（或同等配置）类型，它足以应对低/中等流量。如果你预计会有高流量，则应在此基础上进行扩展。

## 部署 OpenReplay

```bash
## From your terminal
SSH_KEY=~/Downloads/openreplay-key.pem #! wherever you've saved the SSH key
INSTANCE_IP=REPLACE_WITH_INSTANCE_PUBLIC_IP
chmod 400 $SSH_KEY
ssh -i $SSH_KEY ubuntu@$INSTANCE_IP
```

2. 通过提供 OpenReplay 将运行的域名来安装它（例如 DOMAIN_NAME=openreplay.mycompany.com）：

```bash
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/openreplay/openreplay/main/scripts/docker-compose/docker-install.sh)"
```

现在一切就绪，OpenReplay 应该可以通过你最初创建的域名安全地访问。你可以访问 `/signup` 页面（即 openreplay.mycompany.com/signup）来创建账户。

## 有疑问？

如果你遇到任何问题，请加入我们的 [Slack](https://slack.openreplay.com) 或查看我们的[论坛](https://forum.openreplay.com)，从我们的社区获得帮助。
