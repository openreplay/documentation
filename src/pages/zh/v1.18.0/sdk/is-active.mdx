---
title: "JavaScript SDK - isActive 方法"
metaTitle: "JavaScript SDK - isActive 方法"
metaDescription: "关于 OpenReplay SDK 中 isActive 方法的详细信息"
---

此函数可用于判断某个特定的追踪器实例是否处于活动状态并正在录制。

## 签名

```js
isActive(): boolean
```

### 返回值

如果追踪器处于活动状态且会话正在录制，则返回 `true`，否则返回 `false`。
