---
title: "React Native SDK - 方法"
metaTitle: "React Native SDK - 方法"
metaDescription: "React Native SDK 的公共方法列表"
---
import Aside from '~/components/Aside.astro'
import ORCard from '~/components/Card/ORCard.astro'

## 方法

- [startSession()](/rn-sdk/start)：开始录制。
- [setMetadata()](/rn-sdk/set-metadata)：设置一个元数据字段，并将其作为录制的一部分保存。
- [event()](/rn-sdk/event)：使用此方法发送自定义事件，使其作为回放的一部分被记录下来。
- [setUserID()](/rn-sdk/set-user-id)：使用此方法识别您的用户。
- [patchNetwork()](/rn-sdk/patch-network)：修补 fetch 和/或 XMLHttpRequest 以记录网络请求。


```js
interface IORTrackerConnector {
  startSession: (
    projectKey: string,
    optionsDict: Options,
    projectUrl?: string
  ) => void;
  setMetadata: (key: string, value: string) => void;
  event: (name: string, payload?: string) => void;
  setUserID: (userID: string) => void;
  userAnonymousID: (userID: string) => void;
}
```
## 有疑问？
如果您对此流程有任何疑问，欢迎通过我们的 [Slack](https://slack.openreplay.com) 联系我们，或查看我们的[论坛](https://forum.openreplay.com)。