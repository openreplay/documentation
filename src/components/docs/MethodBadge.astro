---
// API method / status badge (design ".or-method").
// variant: post | get | put | del | ok | err
export interface Props {
	variant?: 'post' | 'get' | 'put' | 'del' | 'ok' | 'err';
}
const { variant = 'post' } = Astro.props as Props;
---

<span class={`or-method ${variant}`}><slot /></span>
