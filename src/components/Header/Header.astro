---
import SkipToContent from './SkipToContent.astro';
import SidebarToggle from './SidebarToggle';
import LanguageSelect from './LanguageSelect';
import ThemeToggleButton from './ThemeToggleButton';
import { getLanguageFromURL } from '../../util';
import { useTranslations, getDocSearchStrings } from '../../i18n/util';
import ORLogo from './ORLogo.astro';
import AiChatBot from '../AiChatBot/AiChatBot';
import Search from '../Header/Search.astro';

const { currentPage } = Astro.props;
const lang = currentPage && getLanguageFromURL(currentPage);
const t = useTranslations(Astro);
const docsearchStrings = getDocSearchStrings(Astro);
// GitHub stars — refreshed periodically; shields badge in the right sidebar shows the live count.
const stars = '12.1k';
---

<header class="or-header">
	<SkipToContent />
	<Search labels={docsearchStrings} lang={lang} />

	<a class="or-logo" href={`/${lang || 'en'}/home`}>
		<ORLogo />
		<span class="sr-only">OpenReplay</span>
	</a>
	<span class="or-doc-badge" aria-hidden="true">DOCS</span>

	<div class="or-header-spacer"></div>

	<!-- Search pill (desktop) -->
	<button type="button" id="or-search-trigger" class="or-search-pill hidden lg:flex" aria-label={docsearchStrings.button || 'Search documentation'}>
		<svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><circle cx="11" cy="11" r="7"></circle><path d="m20 20-3-3"></path></svg>
		<span class="or-search-pill-label">Search documentation</span>
		<span class="or-kbd">⌘K</span>
	</button>

	<div class="or-hdr-divider hidden lg:block"></div>

	<!-- GitHub stars -->
	<a class="or-hdr-pill hidden lg:inline-flex" href="https://github.com/openreplay/openreplay" target="_blank" rel="noreferrer" aria-label="OpenReplay on GitHub">
		<svg width="15" height="15" viewBox="0 0 24 24" fill="currentColor"><path d="M12 .5C5.7.5.5 5.7.5 12c0 5.1 3.3 9.4 7.9 10.9.6.1.8-.2.8-.6v-2c-3.2.7-3.9-1.4-3.9-1.4-.5-1.3-1.3-1.7-1.3-1.7-1-.7.1-.7.1-.7 1.2.1 1.8 1.2 1.8 1.2 1 1.8 2.7 1.3 3.4 1 .1-.8.4-1.3.7-1.6-2.6-.3-5.3-1.3-5.3-5.7 0-1.3.5-2.3 1.2-3.1-.1-.3-.5-1.5.1-3.1 0 0 1-.3 3.3 1.2a11.5 11.5 0 0 1 6 0C17.3 4.7 18.3 5 18.3 5c.6 1.6.2 2.8.1 3.1.8.8 1.2 1.8 1.2 3.1 0 4.4-2.7 5.4-5.3 5.7.4.4.8 1.1.8 2.2v3.3c0 .4.2.7.8.6 4.6-1.5 7.9-5.8 7.9-10.9C23.5 5.7 18.3.5 12 .5Z"></path></svg>
		<span class="or-stars">{stars}</span>
	</a>

	<!-- Language pill -->
	<div class="or-lang-pill hidden lg:inline-flex" dir="ltr" lang="en">
		{lang && <LanguageSelect lang={lang} client:idle />}
	</div>

	<!-- Theme toggle pill -->
	<div class="hidden lg:inline-flex or-theme-toggle-slot">
		<ThemeToggleButton
			client:idle
			labels={{
				useLight: t('themeToggle.useLight') || 'Light mode',
				useDark: t('themeToggle.useDark') || 'Dark mode',
			}}
		/>
	</div>

	<!-- Try Cloud CTA -->
	<a class="or-cta hidden lg:inline-flex" href="https://app.openreplay.com/" target="_blank" rel="noreferrer">Try Cloud</a>

	<!-- Mobile actions: search icon + burger -->
	<div class="or-mobile-actions lg:hidden">
		<SidebarToggle client:idle />
	</div>

	<AiChatBot client:load />
</header>

<script is:inline>
	(() => {
		const trigger = document.getElementById('or-search-trigger');
		if (trigger) {
			trigger.addEventListener('click', () => window.dispatchEvent(new Event('open-docsearch')));
		}
	})();
</script>
